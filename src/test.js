const myFunction = require('./handlers/power-consumption-report');

const context = {}; // Replace this with whatever context your function expects
const _ = {}; // Replace this with whatever second argument your function expects

myFunction(context, _);